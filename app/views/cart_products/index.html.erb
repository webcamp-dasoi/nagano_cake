<div class="container">
	<div class="row">
		<div class="pull-left">
			<span>ショッピングカート</span>
		</div>
		<div class="pull-right">
			<%= link_to "カートを空にする", "#", class: "btn btn-danger" %>
		</div>
	</div>

	<div class="row">
			<table class="table table-bordered">
				<thead>
					<tr class="info">
						<th>商品名</th>
						<th>単価(税込み)</th>
						<th>数量</th>
						<th>小計</th>
						<th></th>
					</tr>
				</thead>
				<tbody>
					<% @cart_products.each do |cart_product| %>
						<tr>
							<td>
								<%= attchment_image_tag(cart_product, :image, :fill, 50, 50) %>
								<%= cart_product.product.name %>
							</td>
							<td><%= cart_product.product.non_tax_price * 1.1 %></td>
							<td>
								<%= form_with model: cart_product do |f| %>
									<%= f.text_field :quantity %>
									<%= f.submit "変更", class: "btn btn-primary" %>
								<% end %>
							</td>
							<td>
								<%= cart_product.product.non_tax_price * cart_product.quantity * 1.1 %>
							</td>
							<td>
								<%= link_to "削除する", "#", class: "btn btn-danger" %>
							</td>
						</tr>
					<% end %>
				</tbody>
			</table>
	</div>

	<div class="row">
		<div class="pull-left">
			<%= link_to "買い物を続ける", products_path, class: "btn btn-primary" %>
		</div>
		<div class="pull-right">
			<table class="table table-bordered">
				<thead>
					<tr>
						<td class="info">合計金額</td>
						<td><%= @cart_products.to_a.sum { |product| product.total_price } %></td>
					</tr>
				</thead>
			</table>
		</div>
	</div>

	<div class="row">
		<div class="text-center">
				<%= link_to "情報入力に進む", new_order_path, class: "btn btn-success btn-lg" %>
		</div>
	</div>


</div>